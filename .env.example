ANTHROPIC_API_KEY=sk-ant-...
GOOGLE_API_KEY=
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us-central1
MATHPIX_APP_KEY=
MATHPIX_APP_ID=
# Path to a Google service-account JSON key, used to log each run's cost into a Google Sheet.
# Create the service account in Google Cloud, enable the Google Sheets API, then share the
# target sheet (Editor) with the service account's client_email.
GOOGLE_SERVICE_ACCOUNT_JSON=
